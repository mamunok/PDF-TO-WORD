<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PDF to Word Converter | Free Online Tool</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f7fb;
      text-align: center;
      margin: 0;
      padding: 0;
    }
    .container {
      margin: 100px auto;
      padding: 30px;
      max-width: 600px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }
    h1 {
      color: #4a6cf7;
    }
    input[type="file"] {
      margin: 20px 0;
    }
    button {
      background: #4a6cf7;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
    }
    button:hover {
      background: #364fc7;
    }
    #result {
      margin-top: 20px;
      font-size: 18px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📄 PDF to Word Converter</h1>
    <input type="file" id="pdfFile" />
    <br>
    <button onclick="convertPDF()">Convert to Word</button>
    <div id="result"></div>
  </div>

  <script>
    async function convertPDF() {
      let fileInput = document.getElementById("pdfFile").files[0];
      if (!fileInput) {
        alert("⚠️ Please select a PDF file first!");
        return;
      }

      document.getElementById("result").innerHTML = "⏳ Processing...";

      let formData = new FormData();
      formData.append("File", fileInput);

      try {
        let response = await fetch("https://v2.convertapi.com/convert/pdf/to/docx?Secret=U9t7MOReGbDIlqC8hyGjV4QVWjqCQqnE", {
          method: "POST",
          body: formData
        });

        let data = await response.json();

        if (data.Files && data.Files.length > 0) {
          let link = document.createElement("a");
          link.href = data.Files[0].Url;
          link.download = "converted.docx";
          link.innerText = "⬇️ Download Word File";
          document.getElementById("result").innerHTML = "";
          document.getElementById("result").appendChild(link);
        } else {
          document.getElementById("result").innerHTML = "❌ Conversion failed!";
        }
      } catch (error) {
        document.getElementById("result").innerHTML = "⚠️ Error: " + error.message;
      }
    }
  </script>
</body>
</html>
